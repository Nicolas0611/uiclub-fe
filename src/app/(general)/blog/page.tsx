import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Blog",
  description:
    "UI blog you can find all the latest articles and news about UI/UX",
};
function BlogPage() {
  return <p>BlogPage</p>;
}

export default BlogPage;
